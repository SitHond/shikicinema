import { Routes } from '@angular/router';

import { SupportPage } from '@app/modules/support/support.page';

export const SUPPORT_ROUTES: Routes = [
    {
        path: '',
        component: SupportPage,
    },
];
