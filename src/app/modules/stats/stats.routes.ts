import { Routes } from '@angular/router';

import { StatsPage } from '@app/modules/stats/stats.page';

export const STATS_ROUTES: Routes = [
    {
        path: '',
        component: StatsPage,
    },
];
